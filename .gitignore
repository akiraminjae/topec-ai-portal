# secrets — never commit
.env
**/.env
!.env.example

# node
node_modules/
.next/
frontend/.next/
*.tsbuildinfo

# python
__pycache__/
*.pyc
.venv/

# 로컬 SQLite 폴백 DB (DATABASE_URL 미설정 시 생성됨) — 운영은 Postgres 사용
*.db

# os
.DS_Store

# 로컬 Claude Code 도구 상태 (머신·세션별 설정/락파일 — 팀과 공유하지 않음)
.claude/
